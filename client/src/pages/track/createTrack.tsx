import React from 'react';
import Layout from "@/components/Layout";

const CreateTrack = () => {
    return (
        <Layout>
            <div>Create</div>
        </Layout>
    );
};

export default CreateTrack;